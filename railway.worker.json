{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run db:generate"
  },
  "deploy": {
    "startCommand": "npm run worker:dev",
    "restartPolicyType": "ON_FAILURE"
  }
}
